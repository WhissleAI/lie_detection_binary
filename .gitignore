# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.ipynb_checkpoints/

# Environment / secrets
.env

# Generated data & artifacts — fully regenerable via the pipeline, never committed
data/
*.parquet
*.joblib

# MediaPipe model bundles — downloaded on demand by fatigue/ear.py, never committed
fatigue/models/
*.task

# OS
.DS_Store

# pytest
.pytest_cache/

# generated paper PDFs (regenerate via marketing_post_agent) — but keep the final paper
paper/*.pdf
paper/figures/*.pdf
!paper/deception_detection.pdf
paper/arxiv/
