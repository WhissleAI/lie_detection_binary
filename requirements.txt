# Core scientific stack
numpy<2                # librosa/numba stack is happiest on numpy<2
scipy>=1.10,<1.14
pandas>=2.0
scikit-learn>=1.3
pyarrow>=14.0          # parquet feature matrix

# Gateway calls (STT + audio-visual via POST /video/analyze)
httpx>=0.27

# Audio prosody features (complementary local acoustic lane)
librosa>=0.10
soundfile>=0.12

# Tooling / plotting / progress / tests
python-dotenv>=1.0
tqdm>=4.66
joblib>=1.3
matplotlib>=3.7
pytest>=7.4

# NOTE: visual + STT features come from the Whissle gateway's /video/analyze,
# so no local MediaPipe/OpenCV is required. ffmpeg/ffprobe must be on PATH for
# the prosody lane's audio decode (brew install ffmpeg / apt install ffmpeg).
