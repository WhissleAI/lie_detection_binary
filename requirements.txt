# Core scientific stack
# NOTE: mediapipe (audio-visual lane) requires numpy < 2, so we pin it here.
numpy<2
scipy>=1.10,<1.14
pandas>=2.0
scikit-learn>=1.3
pyarrow>=14.0          # parquet feature matrix

# Audio-visual feature extraction (drives Audio-visual-understanding/LocalVisionEngine)
opencv-python>=4.9
mediapipe>=0.10.14

# Audio prosody features
librosa>=0.10
soundfile>=0.12

# Whissle STT client (also installed editable from ../whissle_python_api by setup.sh)
httpx>=0.27

# Optional: Claude semantic visual lane + any LLM text features
anthropic>=0.40

# Tooling / plotting / progress
python-dotenv>=1.0
tqdm>=4.66
joblib>=1.3
matplotlib>=3.7
